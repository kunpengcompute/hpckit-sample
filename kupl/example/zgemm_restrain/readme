GEMM 结果矩阵按行规约用例，该用例分为kupl并行实现和omp并行实现

编译命令: make 

运行命令: 
    1. 通过make执行OMP实现的测试用例: make run_omp
    2. 通过make执行OMP实现的测试用例: make run_kupl
    3. 通过二进制benchmark执行测试用例, 五个参数依次是矩阵规模M、N、K、线程数、是否检测结果(0标识不检查, 1标识检查): ./benchmark_kupl 65536 32768 256 38 0

清理命令: make clean

用例算子描述
    算子逻辑: 依据gemm分块计算原理, 以最小的block为粒度, 对矩阵乘的的结局矩阵C按照行维度规约, 不保留C矩阵, 从而提升M、N超大时规约的性能


