局部依赖场景下parallel for和graph编程的性能差异

编译命令: make

运行命令: make run

清理命令: make clean

基本概念
    KUPL众核并行模块提供 parallel for 和 计算图 两种并行加速方式，两种方式在特定负载不均衡场景下存在一定性能差异。
    其中 计算图编程 方式通过对于task任务细粒度的依赖编排，可以实现更好的负载均衡效果。

场景介绍
    现在存在长度为N的数组D/A/B/C，其中D为输出数组，A/B/C为输入数组，具体数字中元素按照下述计算表达式进行计算。
    计算表达式：D[i] = A[i] * B[i] * i + C[i] * (N - i)
    针对上述问题可以拆分成如下两个并行循环：
    1、计算 E[i] = A[i] * B[i] * i
    2、计算 D[i] = E[i] + C[i] * (N - i)
    从拆分的两个并行循环可以看到针对下标i而言，步骤1、2是存在依赖关系的，只有在步骤1执行完后才能执行步骤2；
    除此之外步骤1特征为i越小计算量越小，而步骤2特征恰恰相反。