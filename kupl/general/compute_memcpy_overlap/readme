计算数据拷贝隐藏

编译命令: make

运行命令: make run

清理命令: make clean

基本概念
    KUPL 实现了异步数据拷贝，在下发数据拷贝任务后，不必等待拷贝完成。
    异步数据拷贝能够实现数据拷贝和计算的相互隐藏，提高资源利用率。
    KUPL 底层通过 SDMA 设备实现异步数据拷贝。
    计算任务与数据拷贝任务存在数据依赖的场景下，通过拆分任务，同样能够实现数据拷贝和计算的相互隐藏，提高资源利用率。

推荐场景
    符合以下特性的问题可采用异步数据拷贝进行解决：
    1、计算任务与数据拷贝任务不存在数据依赖，希望达到更高的资源利用率
    2、计算任务与数据拷贝任务存在数据依赖，但能够拆分

场景示例
    具体将对计算任务与数据拷贝任务存在数据依赖、但能够拆分的示例进行说明：
    1、程序需要完成 A->B 的拷贝与 B->C 的计算
    2、将拷贝与计算任务分别拆分，将拷贝任务拆分为T1,T2……Tn；将计算任务拆分为K1,K2……Kn
    3、上述任务拆分后，Ki依赖于Ti，因此可通过Ti+1与Ki任务的相互隐藏提高资源利用率