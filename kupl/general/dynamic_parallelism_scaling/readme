动态并发度伸缩 parallel for实现

编译命令: make

运行命令: make run

清理命令: make clean

基本概念
    KUPL 的 parallel for 并行接口是实现多线程编程的一种方法，
    实现类 OMP parallel for 导语能力，多线并行加速 for 循环操作，并提供静态和动态两种策略。
    KUPL 的 egroup 定义了计算资源的集合，通过 borrow、return 和 reset 操作可以实现计算资源的动态伸缩。

推荐场景
    多个并行算子并行执行，且同一层级算子不均衡的场景

场景示例
    示例演示了两个并行的算子a, b。b比a先完成，然后将计算资源释放给b的场景。
