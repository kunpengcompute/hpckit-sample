矩阵转置————int8精度Copy转置

编译命令: make

运行命令: make run

清理命令: make clean

基本概念
    我们已知 MMA 矩阵乘加操作行为，也了解其是矩阵乘 GEMM 运算的核心逻辑。
    在通过 MMA 加速 GEMM 运算的过程中，由于矩阵排布等影响，常常需要执行矩阵元素排布转置的操作。
    KUPL 基于鲲鹏硬件矩阵计算的能力提供了高效的 Copy转置 模板库编程接口，
    通过提供 Copy ops atom + atom shape 的设计方式扩展底层矩阵计算能力提供一系列模板库编程接口从而支持用户快速高效的使能鲲鹏硬件的底层能力进行矩阵GEMM等场景的高效开发

推荐场景
    符合以下特性的问题均可采用 Copy 进行解决
    1、问题涉及矩阵乘行为
    2、问题需要算子融合操作进行细粒度优化

场景示例
    具体以 Copy ops atom 为 COPY_16x4_INT8_RM2ZZ 的 16*576 int8 精度矩阵Copy转置为例进行说明